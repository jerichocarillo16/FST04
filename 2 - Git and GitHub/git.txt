Git version
git --version

git config --global user.name "username"
git config --global user.email "email"
git config user.name
git config user.email

// Initialized into a Git Repository
git init


// Git Three Stages

//add files to the staging area
git add.

//git commit -m "Message"


//Add a branc to our Repository
git branch "firstbatch"

// list of all branches
git branch -a

//switching to a different branch
git checkout "branch_name'

//merging branch to the main branch
git merge firstbranch

//removing branch
git branch -d [branchname]

// to clear on git bash terminal

// Connecting to Github
git remote add origin [websitename]

//changing it to main for cosistency
git branch -M main

//Pushes our local repository to remote repository --Need to use after creating the remote repository
git push -u origin

//Pushed to our remote repository
git push

clear